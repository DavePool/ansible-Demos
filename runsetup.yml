---
- hosts: ansible-engine

  vars_prompt:
          - name: "rhn"
            prompt: "Enter the rhn user "
            private: no
          - name: "password"
            prompt: "Enter your password "
            private: yes
  roles:
          - ansible-engine
